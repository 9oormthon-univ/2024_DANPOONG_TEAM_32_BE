spring:
  application:
    name: onchung

  profiles:
    active: local, common

---
spring:
  config:
    activate:
      on-profile: local

---
spring:
  config:
    activate:
      on-profile: prod